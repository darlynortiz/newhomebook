<%= form_for(@comment_user) do |f| %>
  <% if @comment_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment_user.errors.count, "error") %> prohibited this comment_user from being saved:</h2>

      <ul>
      <% @comment_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message %>
  </div>
    
    <%= f.hidden_field :user_id, :value => @user.id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
